#ifdef COMPBR
if (mc_Entity.x == 20000) { // End Ore
	specR = 12.065;
}
if (mc_Entity.x == 12345) // Lime Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.095, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.65, 0.7, 0.55);
	
if (mc_Entity.x == 12346) // Orange Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.105, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.78, 0.72, 0.61);
	
if (mc_Entity.x == 12347) // Cyan Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.0825, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.675, 0.7, 0.79);
	
if (mc_Entity.x == 12348) // Soul Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.025, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.675, 0.7, 0.74);
	
if (mc_Entity.x == 12349) // Blue Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.13, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.6, 0.6, 0.69);
	
if (mc_Entity.x == 12350) // Purple Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.19, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.9, 0.675, 0.9);
	
if (mc_Entity.x == 12351) // Black Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.99, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(1.83, 1.83, 1.83);
	
if (mc_Entity.x == 12352) // Gray Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.08, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(1.8, 1.8, 1.8);
	
if (mc_Entity.x == 12353) // Light Blue Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.0175, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.9, 0.9, 1.0);
	
if (mc_Entity.x == 12354) // Yellow Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.28, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.5, 0.425, 0.35);

if (mc_Entity.x == 12355) // Magenta Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.045, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.97, 0.7, 0.8);
	
if (mc_Entity.x == 12356) // Pink Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.025, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(1.0, 0.85, 0.85);
	
if (mc_Entity.x == 12357) // Red Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.4, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.69, 0.67, 0.67);

if (mc_Entity.x == 12358) // Green Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.095, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.6, 0.75, 0.6);

if (mc_Entity.x == 12360) // Light Gray Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.0225, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(1.6, 1.6, 1.6);

if (mc_Entity.x == 12361) // White Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.01, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(1.6, 1.6, 1.6);

if (mc_Entity.x == 12362) // Brown Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.14, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.72, 0.6775, 0.675);

if (mc_Entity.x == 12363) // Neon Equisetum
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.05, mat = 2.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.9, 0.6, 0.8);

if (mc_Entity.x == 12364) // Neon Cactye
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.01, mat = 2.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.8, 0.6, 0.9);

if (mc_Entity.x == 12365) // Rainbow Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.065, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.75, 0.75, 0.75);

if (mc_Entity.x == 12366) // Bright Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.005, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.75, 0.75, 0.75);

if (mc_Entity.x == 12367) // Purple Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.06, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.9, 0.675, 0.9);
    
if (mc_Entity.x == 12368) // Brown Glowstone
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.92, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.8, 0.73, 0.73);

if (mc_Entity.x == 12369) // White Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 0.0,
    #endif
    lmCoord.x = 0.87, specB = 3.03, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.75, 0.75, 0.75);

if (mc_Entity.x == 12370) // Light Gray Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 0.0,
    #endif
    lmCoord.x = 0.87, specB = 3.45, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.35, 0.35, 0.35);

if (mc_Entity.x == 12371) // Gray Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 0.0,
    #endif
    lmCoord.x = 0.87, specB = 3.25, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.4, 0.4, 0.4);

if (mc_Entity.x == 12372) // Orange Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.205, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.78, 0.72, 0.61);

if (mc_Entity.x == 12373) // Yellow Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.72, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.5, 0.425, 0.35);

if (mc_Entity.x == 12374) // Pink Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.085, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(1.0, 0.85, 0.85);

if (mc_Entity.x == 12375) // Blue Lamp
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.43, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.6, 0.6, 0.69);

if (mc_Entity.x == 12376) // Low Bright Emissive
    #ifdef NOISY_TEXTURES
        noiseVarying = 1.0,
    #endif
    #ifdef COLORED_LIGHT
        lightVarying = 2.0,
    #endif
    lmCoord.x = 0.87, specB = 3.001, mat = 162.0,
    //mipmapDisabling = 1.0,
    color.rgb = vec3(0.75, 0.75, 0.75);
#endif
